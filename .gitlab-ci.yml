stages:
  - build
  - test

build:
  stage: build
  script:
    - gradle build -x test
  tags:
    - gradle

test:
  stage: test
  script:
    - gradle test
  tags:
    - gradle
  artifacts:
    untracked: true
    when: on_failure
    expire_in: 1 day
